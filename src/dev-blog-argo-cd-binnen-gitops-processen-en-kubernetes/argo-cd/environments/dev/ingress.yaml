apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: api-ingress
  namespace: primetester
spec:
  rules:
    - host: localhost
      http:
        paths:
          - pathType: Prefix
            backend:
                service:
                    name: api
                    port:
                      number: 4000
